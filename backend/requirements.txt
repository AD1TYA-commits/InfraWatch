fastapi>=0.115.0
uvicorn[standard]>=0.30.6
pydantic>=2.9.2
pydantic-settings>=2.5.2
sqlalchemy>=2.0.35
psycopg2-binary>=2.9.9
pytest>=8.3.3
httpx>=0.27.2
python-dotenv>=1.0.1
python-multipart>=0.0.9
passlib>=1.7.4
# passlib 1.7.4's bcrypt handler is incompatible with bcrypt>=4.1 (which
# removed the `__about__` attribute passlib probes, and passlib's own
# wrap-bug detection then crashes on newer bcrypt's stricter 72-byte limit).
# Pin below that until passlib ships a fix.
bcrypt>=4.0.0,<4.1
python-jose[cryptography]>=3.3.0

